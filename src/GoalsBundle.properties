# This is the default GoalsBundle.properties file
runAllPageTests = test known as bad and as good values and check if error messages and titles are right
checkAll = open, do all tests
openWithElementsAndTitleCheck = open login page and check eleents and right title presence;
checkLoginLogout = check normal login and logout sequence
checkWrongValues = test known as bad values and check if error messages and titles are right
